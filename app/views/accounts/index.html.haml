.row
  .col-md-12
    .panel.panel-primary.filterable
      .panel-heading
        %h3.panel-title Accounts
        .pull-right
          %button.btn.btn-info.btn-xs#new-account-button{:type => "button"} New Account
      #new-account-form{style:'display:none;'}
        = render 'accounts/form'
      %div.table-responsive
        %table.table.table-condensed.table-hover
          %thead
            //name: nil, phone: nil, address: nil, account_number: nil, website: nil, username: nil, password: nil
            %tr
              %th Account
              %th Website
              %th{colspan:"2"} Actions
          %tbody
            - @accounts.each do |account|
              %tr
                - color = account.color
                %td= link_to account.name, account, :class => "btn btn-info btn-xs btn-block", style: "background-color:#{color};border-color:#{border_color color};border-bottom-color:#{shadow_color color};width:70%"
                %td= account.website
                %td
                  =link_to 'More Info', account, :class => "btn btn-success btn-xs btn-block"
                %td
                  =link_to 'Destroy', account, :method => :delete, data: { confirm: "Deleting account '#{account.name}', Are you sure?" }, :class => "btn btn-danger btn-xs btn-block"
:javascript
  $(document).ready(function(){
    $('#new-account-button').click(function(){
      $button = $('#new-account-button')
      $form = $('#new-account-form')
      $form.toggle();
      if ($form.is(":visible") ){
        $button.html("Cancel")
      } else {
        $button.html("Add Account")
      }
    });
  });

/ %h1 Listing accounts
/ %table
/   %thead
/     %tr
/       %th{:colspan => "3"}
/   %tbody
/     - @accounts.each do |account|
/       %tr
/         %td= account.name
/         %td= link_to 'Show', account
/         %td= link_to 'Edit', edit_account_path(account)
/         %td= link_to 'Destroy', account, method: :delete, data: { confirm: 'Are you sure?' }
/ %br/
/ = link_to 'New Account', new_account_path
/ = link_to 'Back', root_url